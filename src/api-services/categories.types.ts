export interface Category {
  // Replace these with actual properties inside each category object
  id: string;
  name: string;
  description?: string;
  imageUrl?: string;
  createdAt?: string;
  updatedAt?: string;
}
